# generated by ~/.config/theme/theme ({{name}}) -- edit the template in ~/.config/theme/templates
$rosewater = rgb({{rosewater | strip}})
$rosewaterAlpha = {{rosewater | strip}}
$flamingo = rgb({{flamingo | strip}})
$flamingoAlpha = {{flamingo | strip}}
$pink = rgb({{pink | strip}})
$pinkAlpha = {{pink | strip}}
$mauve = rgb({{accent | strip}})
$mauveAlpha = {{accent | strip}}
$red = rgb({{red | strip}})
$redAlpha = {{red | strip}}
$maroon = rgb({{maroon | strip}})
$maroonAlpha = {{maroon | strip}}
$peach = rgb({{peach | strip}})
$peachAlpha = {{peach | strip}}
$yellow = rgb({{yellow | strip}})
$yellowAlpha = {{yellow | strip}}
$green = rgb({{green | strip}})
$greenAlpha = {{green | strip}}
$teal = rgb({{teal | strip}})
$tealAlpha = {{teal | strip}}
$sky = rgb({{sky | strip}})
$skyAlpha = {{sky | strip}}
$sapphire = rgb({{sapphire | strip}})
$sapphireAlpha = {{sapphire | strip}}
$blue = rgb({{blue | strip}})
$blueAlpha = {{blue | strip}}
$lavender = rgb({{lavender | strip}})
$lavenderAlpha = {{lavender | strip}}
$text = rgb({{text | strip}})
$textAlpha = {{text | strip}}
$subtext1 = rgb({{subtext1 | strip}})
$subtext1Alpha = {{subtext1 | strip}}
$subtext0 = rgb({{subtext0 | strip}})
$subtext0Alpha = {{subtext0 | strip}}
$overlay2 = rgb({{overlay2 | strip}})
$overlay2Alpha = {{overlay2 | strip}}
$overlay1 = rgb({{overlay1 | strip}})
$overlay1Alpha = {{overlay1 | strip}}
$overlay0 = rgb({{overlay0 | strip}})
$overlay0Alpha = {{overlay0 | strip}}
$surface2 = rgb({{surface2 | strip}})
$surface2Alpha = {{surface2 | strip}}
$surface1 = rgb({{surface1 | strip}})
$surface1Alpha = {{surface1 | strip}}
$surface0 = rgb({{surface0 | strip}})
$surface0Alpha = {{surface0 | strip}}
$base = rgb({{base | strip}})
$baseAlpha = {{base | strip}}
$mantle = rgb({{mantle | strip}})
$mantleAlpha = {{mantle | strip}}
$crust = rgb({{crust | strip}})
$crustAlpha = {{crust | strip}}
$accent = rgb({{accent | strip}})
$accentAlpha = {{accent | strip}}
$on_accent = rgb({{on_accent | strip}})
$on_accentAlpha = {{on_accent | strip}}
$accent_container = rgb({{accent_container | strip}})
$accent_containerAlpha = {{accent_container | strip}}
$secondary = rgb({{secondary | strip}})
$secondaryAlpha = {{secondary | strip}}
