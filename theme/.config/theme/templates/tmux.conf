# generated by ~/.config/theme/theme ({{name}}) -- edit the template in ~/.config/theme/templates
set -g @thm_bg "{{base}}"
set -g @thm_fg "{{text}}"
set -g @thm_crust "{{crust}}"
set -g @thm_mantle "{{mantle}}"
set -g @thm_surface_0 "{{surface0}}"
set -g @thm_surface_1 "{{surface1}}"
set -g @thm_surface_2 "{{surface2}}"
set -g @thm_overlay_0 "{{overlay0}}"
set -g @thm_overlay_1 "{{overlay1}}"
set -g @thm_overlay_2 "{{overlay2}}"
set -g @thm_subtext_0 "{{subtext0}}"
set -g @thm_subtext_1 "{{subtext1}}"
set -g @thm_rosewater "{{rosewater}}"
set -g @thm_flamingo "{{flamingo}}"
set -g @thm_pink "{{pink}}"
set -g @thm_mauve "{{accent}}"
set -g @thm_red "{{red}}"
set -g @thm_maroon "{{maroon}}"
set -g @thm_peach "{{peach}}"
set -g @thm_yellow "{{yellow}}"
set -g @thm_green "{{green}}"
set -g @thm_teal "{{teal}}"
set -g @thm_sky "{{sky}}"
set -g @thm_sapphire "{{sapphire}}"
set -g @thm_blue "{{blue}}"
set -g @thm_lavender "{{lavender}}"

set -g status-style "bg={{mantle}},fg={{text}}"
set -g message-style "bg={{surface0}},fg={{text}}"
set -g mode-style "bg={{accent}},fg={{on_accent}}"
set -g pane-border-style "fg={{surface1}}"
set -g pane-active-border-style "fg={{accent}}"
set -g window-status-current-style "fg={{accent}},bold"
